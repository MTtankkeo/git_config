## 1.0.0
- Initial version.

## 1.1.0
- Updated fetch behavior to warn when files not tracked by `.gitignore` are fetched and to optionally add them automatically to `.gitignore` to prevent accidental commits.
